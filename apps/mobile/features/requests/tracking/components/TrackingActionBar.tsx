import { Ionicons } from "@expo/vector-icons";
import { Pressable, StyleSheet, Text, View } from "react-native";
export function TrackingActionBar({ onCallHotline, hotline, onContactSupport, resolved = false }: { onCallHotline: () => void; hotline: string; onContactSupport?: () => void; resolved?: boolean }) { return <View style={styles.card}>{onContactSupport ? <Pressable onPress={onContactSupport} style={styles.secondary}><Ionicons name="headset-outline" size={21} color="#DC2626" /><Text style={styles.secondaryText}>Contact Support</Text></Pressable> : null}<Pressable onPress={onCallHotline} style={[styles.primary, !onContactSupport && { flex: 1 }, resolved && styles.resolved]} accessibilityRole="button" accessibilityHint={`Opens the phone dialer with ${hotline}`}><Ionicons name="call-outline" size={22} color="#FFF" /><Text style={styles.primaryText}>{resolved ? `Emergency Hotline ${hotline}` : "Call Emergency Hotline"}</Text></Pressable></View>; }
const styles = StyleSheet.create({ card: { marginHorizontal: 16, marginBottom: 8, flexDirection: "row", gap: 10, borderRadius: 22, borderWidth: 1, borderColor: "#E2E8F0", backgroundColor: "#FFF", padding: 12, shadowColor: "#0F172A", shadowOpacity: 0.05, shadowRadius: 12, elevation: 2 }, secondary: { flex: 1, minHeight: 54, borderRadius: 15, borderWidth: 1, borderColor: "#FCA5A5", flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 8 }, secondaryText: { fontSize: 13, fontWeight: "900", color: "#DC2626" }, primary: { flex: 1.25, minHeight: 54, borderRadius: 15, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 8, backgroundColor: "#DC2626", elevation: 2 }, resolved: { backgroundColor: "#475569" }, primaryText: { fontSize: 14, fontWeight: "900", color: "#FFF" } });
