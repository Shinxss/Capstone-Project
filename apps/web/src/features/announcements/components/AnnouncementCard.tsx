import { CalendarClock, Eye, Pencil, Send, Trash2, Undo2, Users } from "lucide-react";
import type { Announcement } from "../models/announcements.types";
import { announcementPreview, audienceLabel, formatAnnouncementDate } from "../utils/announcementDashboard.utils";
import AnnouncementStatusBadge from "./AnnouncementStatusBadge";

type Props = { announcement: Announcement; busy: boolean; onView: (item: Announcement) => void; onEdit: (item: Announcement) => void; onPublish: (item: Announcement) => void; onUnpublish: (item: Announcement) => void; onDelete: (item: Announcement) => void };

export default function AnnouncementCard({ announcement, busy, onView, onEdit, onPublish, onUnpublish, onDelete }: Props) {
  return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:border-slate-300 hover:shadow-md dark:border-[#1C2940] dark:bg-[#0B1220] dark:hover:border-[#2A3A56] sm:p-5"><div className="flex flex-col gap-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="text-base font-black text-slate-950 dark:text-white">{announcement.title}</h2><AnnouncementStatusBadge status={announcement.status} /></div><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">{announcementPreview(announcement.body)}</p></div><span className="inline-flex shrink-0 items-center gap-1.5 self-start rounded-full bg-slate-100 px-3 py-1.5 text-xs font-bold text-slate-600 dark:bg-[#162238] dark:text-slate-300"><Users size={13} /> {audienceLabel(announcement.audience)}</span></div><div className="flex flex-col gap-3 border-t border-slate-100 pt-3 sm:flex-row sm:items-center sm:justify-between dark:border-[#1C2940]"><div className="flex flex-wrap gap-x-4 gap-y-1 text-xs text-slate-500"><span className="inline-flex items-center gap-1"><CalendarClock size={13} /> Created {formatAnnouncementDate(announcement.createdAt)}</span>{announcement.publishedAt ? <span>Published {formatAnnouncementDate(announcement.publishedAt)}</span> : null}</div><div className="flex flex-wrap gap-2"><button type="button" onClick={() => onView(announcement)} className="inline-flex h-9 items-center gap-1.5 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-700 hover:bg-slate-50 dark:border-[#24324A] dark:text-slate-200 dark:hover:bg-[#122036]"><Eye size={14} /> View</button><button type="button" onClick={() => onEdit(announcement)} className="inline-flex h-9 items-center gap-1.5 rounded-lg border border-slate-200 px-3 text-xs font-bold text-slate-700 hover:bg-slate-50 dark:border-[#24324A] dark:text-slate-200 dark:hover:bg-[#122036]"><Pencil size={14} /> Edit</button>{announcement.status === "PUBLISHED" ? <button type="button" disabled={busy} onClick={() => onUnpublish(announcement)} className="inline-flex h-9 items-center gap-1.5 rounded-lg border border-amber-200 px-3 text-xs font-bold text-amber-700 hover:bg-amber-50 disabled:opacity-50 dark:border-amber-500/30 dark:text-amber-300"><Undo2 size={14} /> Unpublish</button> : <button type="button" disabled={busy} onClick={() => onPublish(announcement)} className="inline-flex h-9 items-center gap-1.5 rounded-lg bg-emerald-600 px-3 text-xs font-bold text-white hover:bg-emerald-700 disabled:opacity-50"><Send size={14} /> Publish</button>}<button type="button" aria-label={`Delete ${announcement.title}`} disabled={busy} onClick={() => onDelete(announcement)} className="grid size-9 place-items-center rounded-lg border border-red-200 text-red-600 hover:bg-red-50 disabled:opacity-50 dark:border-red-500/30 dark:text-red-300"><Trash2 size={14} /></button></div></div></div></article>;
}
